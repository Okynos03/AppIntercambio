<resources>
    <style name="Theme.AppIntercambio" parent="Theme.Material3.Light.NoActionBar">
        <!-- Colores principales -->
        <item name="colorPrimary">@color/navidad_rojo</item>
        <item name="colorPrimaryDark">@color/navidad_rojo_oscuro</item>
        <item name="colorAccent">@color/navidad_rojo</item>

        <!-- Barra de estado -->
        <item name="android:statusBarColor">@color/navidad_rojo</item>
        <item name="android:windowLightStatusBar">false</item>

        <!-- Referencia a los estilos de los pickers -->
        <item name="android:datePickerDialogTheme">@style/NavidadPickerTheme</item>
        <item name="android:timePickerDialogTheme">@style/NavidadPickerTheme</item>
    </style>
</resources>